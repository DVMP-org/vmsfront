import React from "react";

const CameraHome: React.FC = () => {
    return (
        <div>
            <h1>Camera Admin Dashboard</h1>
            <p>Welcome to the Camera plugin admin home page.</p>
        </div>
    );
};

export default CameraHome;
